{
  "chapters": [
    {
      "id": "practice",
      "title": "实战演练",
      "sections": [
        {
          "title": "背景知识：Memory 模块的定位——Soul 与 Storage 之间的桥梁",
          "anchor": "practice-memory-background",
          "groupTitle": "Memory 记忆管理",
          "difficulty": "beginner",
          "estimatedMinutes": 10,
          "blocks": [
            {
              "type": "text",
              "text": "`memory/manager.py`（1185 行）是 OpenBiliClaw 的**记忆管理系统**。它在 Soul Engine（理解层）和 Database（存储层）之间扮演桥梁角色——将原始行为事件转化为可供画像消费的结构化记忆。\n\n### 两个容易混淆的概念\n\n| 概念 | 所在模块 | 含义 | 类比 |\n|------|---------|------|------|\n| **五层灵魂**（Soul Profile） | `soul/` | 内容层面的分层理解——从事件到人格 | 你对一个人的认知：行为→喜好→性格→价值观 |\n| **多层记忆**（Memory Layers） | `memory/` | 存储层面的数据管理策略 | 你的大脑：短期记忆→工作记忆→长期记忆 |\n\n五层灵魂回答的是「如何理解用户」，多层记忆回答的是「如何组织和持久化这些数据」。"
            },
            {
              "type": "diagram",
              "diagram": {
                "language": "mermaid",
                "content": "flowchart LR\n    subgraph TOTAL [OpenBiliClaw 总架构]\n        SRC[sources/]\n        style SRC fill:#e2e8f0,stroke:#94a3b8\n        SOUL[soul/<br/>SoulEngine]\n        style SOUL fill:#e2e8f0,stroke:#94a3b8\n        DISC[discovery/]\n        style DISC fill:#e2e8f0,stroke:#94a3b8\n        REC[recommendation/]\n        style REC fill:#e2e8f0,stroke:#94a3b8\n        API[api/]\n        style API fill:#e2e8f0,stroke:#94a3b8\n        STORE[storage/<br/>SQLite 44表]\n        style STORE fill:#e2e8f0,stroke:#94a3b8\n    end\n\n    subgraph MEMORY [memory/ · 1,185 行 · 📖 本教程]\n        MM[MemoryManager<br/>17 种事件类型<br/>信号强度权重]\n        ML[MemoryLayer<br/>懒加载 · 脏检查<br/>UTF-8 强制]\n        DM[发现运行时记忆<br/>probe_feedback<br/>avoidance_history]\n    end\n    style MEMORY fill:#fef3c7,stroke:#f59e0b,stroke-width:3px\n\n    SRC -->|行为事件| MM\n    MM -->|结构化记忆| SOUL\n    SOUL -->|画像信号| DISC\n    MM -->|持久化| STORE\n    ML -.->|管理| MM\n    DM -.->|避免重复探索| DISC\n    API -->|事件上报| MM",
                "caption": "Memory 在总架构中的位置：连接 Soul Engine 与 Storage，管理 17 种事件类型的入库与结构化"
              }
            },
            {
              "type": "text",
              "text": "### 支持的事件类型与信号强度\n\n`MemoryManager` 定义了 17 种用户行为事件类型，每种事件有不同的**信号强度**（signal_strength），用于画像更新时的权重计算：\n\n| 事件类型 | 信号强度 | 含义 |\n|---------|---------|------|\n| `view` | 基础 | 浏览了一个内容 |\n| `like` | 较强 | 明确表达喜欢 |\n| `favorite` | 强 | 收藏——最高级别的认可 |\n| `coin` | 强 | B站投币——高成本行为 |\n| `search` | 较强 | 主动搜索——有明确意图 |\n| `dialogue` | 最强 | 直接对话——最精确的偏好表达 |\n| `feedback` | 可变 | 喜欢/不喜欢/多来点 |\n| `follow` | 强 | 关注UP主——长期兴趣信号 |\n| `scroll` / `hover` | 弱 | 浏览行为——可能是偶然 |\n| `reshuffle` | 弱 | 换一批——对当前推荐不满意 |\n\n信号强度的设计哲学：**用户付出的行为成本越高，信号越可靠**。收藏 > 投币 > 点赞 > 浏览，因为收藏需要主动操作，而浏览可能只是误点。"
            }
          ]
        },
        {
          "title": "MemoryLayer：懒加载、脏检查与 UTF-8 强制",
          "anchor": "practice-memory-layer",
          "groupTitle": "Memory 记忆管理",
          "difficulty": "intermediate",
          "estimatedMinutes": 12,
          "blocks": [
            {
              "type": "text",
              "text": "`MemoryLayer` 是记忆系统的基本存储单元。每个 Layer 对应磁盘上的一个 JSON 文件，实现了三个关键机制。\n\n### 1. 懒加载（Lazy Loading）\n\nMemoryLayer 不会在初始化时加载所有数据，而是在首次访问时才从磁盘读取。这避免了启动时的大量 I/O（Input/Output，输入/输出——这里特指磁盘读写操作，磁盘读写比内存访问慢几个数量级），特别是当某些记忆层很少被访问时。\n\n### 2. 脏检查（Stale Detection）\n\n通过 `_loaded_mtime` 跟踪文件的最后修改时间。每次访问前检查文件是否被外部修改（如用户手动编辑、其他进程写入），如果是则自动重载。\n\n### 3. UTF-8 强制编码\n\n代码中有一段关键注释说明了为什么必须显式指定 `encoding=\"utf-8\"`：\n\n> \"Without `encoding='utf-8'` Python uses the platform's locale encoding — which is GBK on Chinese Windows installs — and our JSON files contain Chinese profile text + emoji that GBK can't decode.\"\n\n中文 Windows 的默认编码是 GBK，而画像数据包含中文和 emoji，GBK 无法解码。这个看似微小的细节在国际化部署中是常见的坑。"
            },
            {
              "type": "code",
              "code": {
                "language": "python",
                "content": "# memory/manager.py — MemoryLayer 核心实现\n\nclass MemoryLayer:\n    \"\"\"单个记忆层——对应磁盘上的一个 JSON 文件。\"\"\"\n\n    def __init__(self, name: str, storage_path: Path) -> None:\n        self.name = name\n        self.storage_path = storage_path\n        self._data: dict[str, Any] = {}\n        self._loaded_mtime: float | None = None\n\n    def load(self) -> None:\n        \"\"\"从磁盘加载——强制 UTF-8 编码。\"\"\"\n        if self.storage_path.exists():\n            with open(self.storage_path, encoding=\"utf-8\") as f:\n                self._data = json.load(f)\n            self._loaded_mtime = self.storage_path.stat().st_mtime\n\n    def _reload_if_stale(self) -> None:\n        \"\"\"检查文件是否被外部修改，如果是则自动重载。\"\"\"\n        current_mtime = self.storage_path.stat().st_mtime\n        if self._loaded_mtime is None or current_mtime > self._loaded_mtime:\n            self.load()\n\n    def save(self) -> None:\n        \"\"\"持久化到磁盘——ensure_ascii=False 保留中文和 emoji。\"\"\"\n        with open(self.storage_path, \"w\", encoding=\"utf-8\") as f:\n            json.dump(self._data, f, ensure_ascii=False, indent=2)",
                "caption": "memory/manager.py — MemoryLayer 懒加载与脏检查",
                "lineHighlights": [
                  12,
                  18,
                  23
                ]
              }
            },
            {
              "type": "diagram",
              "diagram": {
                "language": "mermaid",
                "content": "flowchart TB\n    subgraph LAYERS [MemoryLayer 三层记忆模型]\n        WM[Working Memory<br/>当前活跃画像<br/>SoulProfile 内存对象]\n        EM[Episodic Memory<br/>行为事件序列<br/>SQLite events 表]\n        SM[Semantic Memory<br/>结构化知识<br/>memory/*.json 文件]\n    end\n\n    subgraph MECHANISM [MemoryLayer 三大机制]\n        LAZY[懒加载<br/>首次访问才读磁盘]\n        STALE[脏检查<br/>mtime 追踪外部修改]\n        UTF8[UTF-8 强制<br/>encoding=utf-8]\n    end\n\n    subgraph FILES [磁盘 JSON 文件]\n        F1[profile_overrides.json<br/>用户手动修正]\n        F2[cognition_cycle_state.json<br/>认知循环状态]\n        F3[probe_feedback_history.json<br/>推测反馈记录]\n        F4[pipeline_state.json<br/>管道缓冲区]\n    end\n\n    SM -->|管理| FILES\n    LAZY -->|按需读取| SM\n    STALE -->|自动重载| SM\n    UTF8 -->|正确解码| SM\n\n    WM -->|当前画像| SOUL_ENGINE[SoulEngine<br/>画像更新管道]\n    EM -->|事件游标| SOUL_ENGINE\n    SM -->|知识层| SOUL_ENGINE\n\n    EVENTS[新行为事件] -->|写入| EM\n    EVENTS -->|信号唤醒| SOUL_ENGINE",
                "caption": "Memory 三层记忆模型：Working/Episodic/Semantic 对应认知科学标准分类，配合懒加载+脏检查+UTF-8 强制三大机制"
              }
            }
          ]
        },
        {
          "title": "Memory vs RAG：缓存、会话与长期记忆的边界",
          "anchor": "practice-memory-vs-rag",
          "groupTitle": "Memory 记忆管理",
          "difficulty": "advanced",
          "estimatedMinutes": 15,
          "blocks": [
            {
              "type": "text",
              "text": "面试官常问：「RAG 和 Memory 有什么区别？缓存是记忆吗？会话历史等于长期记忆吗？」这些问题的本质是理解**数据生命周期**和**访问模式**的差异。\n\n### RAG vs Memory 的本质区别\n\n| 维度 | RAG | Memory |\n|------|-----|--------|\n| 目的 | 为当前查询找到相关证据 | 持久化状态，跨会话保持 |\n| 生命周期 | 查询结束即释放 | 长期保留，持续更新 |\n| 访问模式 | 按语义相似度检索 | 按时间/身份/状态直接读取 |\n| 数据性质 | 外部知识（文档、内容） | 内部状态（画像、偏好、历史） |\n\nOpenBiliClaw 的划分：embedding_cache.db / pool_purge / consolidator 是 **RAG**（语义检索缓存，可按需重建）；SoulProfile / events 表 / memory/*.json 是 **Memory**（持久化状态，跨会话保持）。\n\n### 缓存 ≠ 记忆\n\n| 维度 | 缓存（Cache） | 记忆（Memory） |\n|------|--------------|---------------|\n| 目的 | 加速重复计算 | 保持状态和知识 |\n| 淘汰策略 | LRU/LFU（按访问频率） | 语义关联（按重要性） |\n| 可重建性 | **可丢失、可重建** | **不可丢失、不可重建** |\n| 内容 | 计算结果的副本 | 提炼后的知识 |\n\nOpenBiliClaw 严格区分两者：\n- **缓存**（可丢失）：L1 内存缓存（进程重启即丢失）、L2 embedding_cache.db（删除后重新计算）、搜索词缓存（6 小时 TTL）\n- **记忆**（不可丢失）：events 表（丢失=丢失画像基础）、memory/*.json（丢失=丢失深层理解）、profile_snapshots（丢失=丢失回溯能力）\n\n把嵌入存储在独立数据库（embedding_cache.db）中，正是为了区分「可重建的缓存」和「不可重建的业务数据」。\n\n### 会话历史 ≠ 长期记忆\n\n| 维度 | 会话历史（Session State） | 长期记忆（Long-term Memory） |\n|------|------------------------|---------------------------|\n| 生命周期 | 单次会话 | 跨会话持久化 |\n| 内容 | 原始对话/事件序列 | 结构化知识（画像、偏好） |\n| 访问方式 | 按时间顺序 | 按语义/身份/状态 |\n| 更新频率 | 每次交互都追加 | 有写入策略（节流、批处理） |\n\nOpenBiliClaw 的区分：\n- **会话历史**：`dialogue_turns` 表（单次对话记录）、当前 SoulProfile 对象（内存中）\n- **长期记忆**：`events` 表（跨会话行为历史）、`memory/*.json`（多层记忆）、`profile_snapshots`（画像版本化快照）\n\n### 为什么 Memory 需要写入策略？\n\nMemory 不是「越多越好」——频繁写入会导致：\n1. **LLM 成本失控**：每次写入可能触发 LLM 调用（Awareness/Insight 分析）\n2. **噪声积累**：少量事件不足以形成有意义的模式\n3. **状态不稳定**：画像频繁变化导致推荐方向摇摆\n\nOpenBiliClaw 的 CognitionCycle 用**双重节流**（12 小时或 30 事件触发）+ **有界批处理**（300 事件/批，上限 900）+ **单次重试**。稳态运行时**零 LLM 调用**——只有积累足够证据时才触发深层分析。\n\n### SoulProfile vs MemoryLayer：画像与记忆的分工\n\n| 维度 | SoulProfile（画像） | MemoryLayer（记忆） |\n|------|-------------------|-------------------|\n| 内容 | 结构化的用户理解（兴趣、风格、特质） | 原始认知产出（假设、推测历史） |\n| 访问方式 | 整体读取，作为 LLM 输入 | 按需加载，按身份检索 |\n| 更新频率 | 每次画像管道运行后 | CognitionCycle 触发时 |\n| 用途 | 驱动推荐和发现 | 支撑画像更新的深层依据 |\n\n协同闭环：MemoryLayer 提供证据 → SoulProfile 提炼为用户理解 → 驱动推荐 → 产生新行为 → CognitionCycle 更新 MemoryLayer。"
            }
          ]
        },
        {
          "title": "记忆系统的设计教训与最佳实践",
          "anchor": "practice-memory-lessons",
          "groupTitle": "Memory 记忆管理",
          "difficulty": "intermediate",
          "estimatedMinutes": 6,
          "blocks": [
            {
              "type": "list",
              "list": {
                "style": "ordered",
                "items": [
                  {
                    "content": "**信号强度不是拍脑袋决定的**——它与用户的行为成本成正比。收藏（需要主动操作）> 点赞（一次点击）> 浏览（可能误触）。",
                    "highlight": true
                  },
                  {
                    "content": "**UTF-8 编码必须显式指定**——中文 Windows 默认 GBK，无法解码包含 emoji 的 JSON。这个坑在国际化部署中极其常见。",
                    "highlight": true
                  },
                  {
                    "content": "**懒加载不是偷懒，而是资源管理**——启动时不需要所有记忆层，按需加载可以减少启动时间和 I/O。",
                    "highlight": false
                  },
                  {
                    "content": "**脏检查支持外部修改**——用户可能手动编辑 JSON 文件来修正画像，系统应能检测并自动重载，而非覆盖用户的修改。",
                    "highlight": false
                  },
                  {
                    "content": "**记忆模块是 Soul 与 Storage 的桥梁**——它不直接做画像更新，而是协调事件从入库到可消费的完整链路。",
                    "highlight": true
                  }
                ]
              }
            }
          ]
        },
        {
          "title": "记忆与画像的协作：从事件到洞察的完整链路",
          "anchor": "practice-memory-pipeline",
          "groupTitle": "Memory 记忆管理",
          "difficulty": "advanced",
          "estimatedMinutes": 12,
          "blocks": [
            {
              "type": "text",
              "text": "MemoryManager 不只是被动的数据存储——它协调着事件从入库到画像更新的完整链路。\n\n### 事件处理流程\n\n```\n浏览器插件上报事件\n        ↓\nMemoryManager.ingest_event()\n        ↓\n1. 事件类型验证（SUPPORTED_EVENT_TYPES）\n        ↓\n2. 信号强度查找（default_signal_strength_for_event）\n        ↓\n3. Database.insert_event()（去重检查 + 写入）\n        ↓\n4. 如果是新事件 → 触发画像更新信号\n        ↓\n5. SoulEngine 消费信号 → 逐层处理\n```\n\n### 发现运行时记忆\n\nMemoryManager 还管理着发现引擎的运行时记忆（`_DISCOVERY_RUNTIME_HISTORY_KEYS`）：\n\n- `probe_feedback_history`：推测兴趣的反馈历史——哪些推测被验证了，哪些被否定了\n- `avoidance_probe_feedback_history`：回避推测的反馈历史\n\n这些记忆以时间戳映射的形式存储（`probed_domains`、`probed_axes`、`probed_distance_bands`），让发现引擎能够记住「已经探索过什么」，避免重复探索，也避免过早放弃。"
            },
            {
              "type": "callchain",
              "callchain": {
                "endpoint": "MemoryManager → SoulEngine 事件处理链路",
                "steps": [
                  {
                    "layer": "Ingest",
                    "filePath": "memory/manager.py",
                    "functionName": "ingest_event()",
                    "explanation": "接收来自浏览器插件或后台 Producer 的行为事件。先验证事件类型是否在 SUPPORTED_EVENT_TYPES 中，然后查找对应的信号强度。",
                    "keyPoint": "事件类型验证是第一道防线——未知类型的事件会被静默丢弃，防止脏数据污染画像"
                  },
                  {
                    "layer": "Persist",
                    "filePath": "storage/database.py",
                    "functionName": "insert_event()",
                    "explanation": "将事件写入 SQLite 的 events 表。通过 ingest_key 做去重检查——同一用户对同一内容的同一操作只记录一次。返回 EventInsertResult 表示是否真正插入。",
                    "keyPoint": "去重是关键——如果同一事件被重复插入，画像更新会被重复触发，导致偏好权重虚高"
                  },
                  {
                    "layer": "Signal",
                    "filePath": "soul/pipeline.py",
                    "functionName": "signal_from_event()",
                    "explanation": "将已入库的事件转化为 ProfileSignal。ProfileSignal 是管道模式的标准化输入，包含事件类型、内容元数据、信号强度等信息。",
                    "keyPoint": "ProfileSignal 是管道模式的「标准件」——所有层都消费同一种输入格式"
                  },
                  {
                    "layer": "Gate",
                    "filePath": "soul/posture_gate.py",
                    "functionName": "PostureGate.gate()",
                    "explanation": "判断信号是否值得处理。低信号强度的事件（如 scroll、hover）可能被过滤掉。这是画像更新的第一道质量关卡。",
                    "keyPoint": "PostureGate 确保只有有意义的行为才能影响画像——噪声行为不会污染用户理解"
                  }
                ]
              }
            }
          ]
        }
      ]
    }
  ]
}